# 小何的每日健身计划

这是一个可以直接上传到 GitHub Pages 的静态健身计划页面。页面使用下载版 `index.html` 和 `styles.css`，包含今日训练打卡展示、周记录、训练可视化和历史周回顾。

## 本地打开

在 Finder 中打开这个文件夹，然后双击 `index.html`。

也可以在终端运行：

```bash
open ~/Desktop/fitness-tracker-site/index.html
```

## 说明

当前版本是静态视觉页面，按钮和数据展示为页面内容，不会自动保存到浏览器或云端。
